package com.start.agent.model;

/**
 * 故事契约类型：主线真源链路 MASTER_SETTING → VOLUME → CHAPTER → REVIEW → CHAPTER_COMMIT。
 */
public enum StoryContractType {
    MASTER_SETTING,
    VOLUME,
    CHAPTER,
    REVIEW,
    CHAPTER_COMMIT
}
